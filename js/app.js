(function(){
  var model, controller, views;

  model = {
    init: function() {
      this.currentLoc = 'Orlando, FL';
    }
  };

  controller = {
    init: function() {
      // Initialize Model
      model.init();
      // Initialize Views
    }
  };

  views = {
    init: function() {
      // Collect DOM elements
    }
  };

  controller.init();

})();
